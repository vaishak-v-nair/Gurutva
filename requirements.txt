# Pinned environment — gravity-posterior (established 2026-07-29, Python 3.14.6)
# The SimPEG pin is load-bearing: reproduction results are validated against
# this exact version. Do not upgrade mid-phase.
simpeg==0.25.2
discretize==0.12.0
numpy==2.5.1
scipy==1.18.0
pandas==3.0.3
matplotlib==3.11.1
pytest==9.1.1
sbi==0.26.1
torch==2.13.0
